# Database

Canonical SQL schemas and migration notes for GVG Global Trade OS.

- `schema.sql` — core relational model (profiles, catalog, RFQ, quotes, orders)
- Apply production migrations through `supabase/migrations/`
