# Docker

```bash
# From repo root
docker compose -f docker/docker-compose.yml build
docker compose -f docker/docker-compose.yml up
```

Enable Next.js standalone output in `next.config.ts` before production image builds.
