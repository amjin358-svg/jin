# Tests

```
tests/
├── unit/          # Node test runner / Vitest-ready unit tests
├── integration/   # API + repository integration (Phase 2)
└── e2e/           # Playwright flows (Phase 2)
```

Run foundation unit checks:

```bash
npm run test
```
